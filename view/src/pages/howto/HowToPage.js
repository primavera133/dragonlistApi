import { jsx } from '@emotion/react'

import React from 'react'
import { Layout } from '../../components/Layout'

import { PageHeader } from '../../components/PageHeader'

export const HowToPage = ({ history }) => {
  return (
    <Layout>
      <PageHeader>How to</PageHeader>
    </Layout>
  )
}
