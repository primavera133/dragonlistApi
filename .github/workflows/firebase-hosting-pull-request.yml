# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
"on": pull_request
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm run build
        env:
          REACT_APP_BASE_URL: https://dragonlist-2aa56.web.app
          REACT_APP_API_KEY: "${{ secrets.FIREBASE_API_KEY }}"
          REACT_APP_AUTHDOMAIN: "${{ secrets.FIREBASE_AUTHDOMAIN }}"
          REACT_APP_DATABASEURL: "${{ secrets.FIREBASE_DATABASEURL }}"
          REACT_APP_PROJECT_ID: "${{ secrets.FIREBASE_PROJECT_ID }}"
          REACT_APP_STORAGEBUCKET: "${{ secrets.FIREBASE_STORAGEBUCKET }}"
          REACT_APP_MESSAGING_SENDER_ID: "${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}"
          REACT_APP_APP_ID: "${{ secrets.FIREBASE_APP_ID }}"
          REACT_APP_MEASUREMENT_ID: "${{ secrets.FIREBASE_MEASUREMENT_ID }}"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_DRAGONLIST_2AA56 }}"
          projectId: dragonlist-2aa56
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
